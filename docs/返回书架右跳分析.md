# 「从阅读页返回书架，封面与右上/右下按钮整体向右跳」根因分析

> 复现环境：阔折叠内屏横屏（宽 ≥ 840dp、高 ≥ 480dp → 外壳为 `NavigationRail` 侧边导航）
> 实测设备：小米阔折叠内屏横屏（导航栏在左缘时表现为向右跳）
> 结论：**外壳 rail 宽度 = 80dp + start 侧系统栏 inset**，返回时该 inset 晚于书架首帧落地。

---

## 1. 现象与几何

书架内容区（封面网格、右上动作按钮、右下 FAB）位于 `NavigationSuiteScaffold` 的 content 槽，
该槽的位置由 rail 宽度决定。material3-adaptive-navigation-suite 1.5.0-alpha28 源码
（`NavigationSuiteScaffoldLayout`）：

```kotlin
// 测量：content 的宽度约束减去 rail 宽度
contentPlaceable = measurables.fastFirst { it.layoutId == ContentLayoutIdTag }
    .measure(constraints.copy(
        minWidth = layoutWidth - (navigationPlaceable.width * animationProgress).toInt(),
        maxWidth = layoutWidth - (navigationPlaceable.width * animationProgress).toInt(),
    ))
// 摆放：content 放在 rail 右侧
contentPlaceable.placeRelative((navigationPlaceable.width * animationProgress).toInt(), 0)
```

而 `NavigationRail` 自身宽度含其 windowInsets：

```kotlin
// NavigationRailDefaults.windowInsets
WindowInsets.systemBarsForVisualComponents.only(WindowInsetsSides.Vertical + WindowInsetsSides.Start)
// material3 内部：systemBarsForVisualComponents = systemBars.union(displayCutout)
```

即 **rail 宽度 = 80dp + start(左) 侧 systemBars∪cutout inset**，`contentX = rail 宽度`。

因此 start 侧 inset 由 0 变为 48dp 时：

- 内容区整体右移 48dp（封面、右上动作、FAB 一起右移）；
- 网格列宽同步变小（`GridCells.Adaptive`），第 1..n 列各自再叠加不同位移 → 视觉上"封面成片向右跳"；
- 顶栏（含右上 4 个动作）与 FAB 位移量最大且方向一致，最显眼。

top 侧（状态栏）晚到则表现为内容整体下移（`LargeTopAppBar` 变高）。

## 2. 触发链

1. 阅读页沉浸：`SystemBarEffects` 用 `BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE` + `hide(systemBars)`
   → 四边 `WindowInsets.systemBars` 归零；
2. 返回时 `show(systemBars)` 由系统**异步**派发，状态栏与导航栏是不同窗口，可能先后到位
   （横屏侧边导航栏常晚于状态栏）；
3. 若在 start 侧 inset 落地前就 pop 回书架：书架首帧 `start=0` → rail = 80dp → 内容整体偏左；
4. 数帧后 inset 落地 → rail 变宽 → **内容整体右跳**（本次实测 72px = 48dp，另含 36px 下移）。

## 3. 实测证据（emulator-5554，`wm size 840x2400 / wm density 240`，ROTATION_270 → 左缘导航栏）

配置等价于阔折叠内屏横屏：1600×560dp、`NavigationRail`、left inset 72px、top inset 36px。

### 3.1 跳变（临时旁路 inset 门控，等价于修复前的构建）

```
leaveReader: EXPERIMENT no-gate, navigate immediately
recompose: route=bookshelf layoutType=NavigationRail
bookshelf: content size=2280 x 612
bookshelf: content pos=Offset(120.0, 228.0) size=2280 x 612   ← rail=120px，left inset 还没到
bookshelf: content size=2280 x 576                            ← top inset 落地
bookshelf: content size=2208 x 576                            ← left inset 落地：rail 120 → 192
bookshelf: content pos=Offset(192.0, 264.0) size=2208 x 576   ← 内容整体右跳 72px、下跳 36px
```

### 3.2 已修复构建（`BarsRestoreGate` 生效）

```
leaveReader: bars shown, expected=InsetsSnapshot(left=72, top=36, right=0, bottom=0)
leaveReader gate frame#1..#3 InsetsSnapshot(left=0, top=0, right=0, bottom=0)
leaveReader gate frame#4 InsetsSnapshot(left=72, top=36, right=0, bottom=0)
leaveReader gate frame#5 InsetsSnapshot(left=72, top=36, right=0, bottom=0)
leaveReader: gate passed after 5 frames, navigate
recompose: route=bookshelf layoutType=NavigationRail
bookshelf: innerPadding=(0.0.dp,176.0.dp,0.0.dp,0.0.dp) books=19
bookshelf: content pos=Offset(192.0, 264.0) size=2208 x 576   ← 首帧即终局
bookshelf: content pos=Offset(192.0, 264.0) size=2208 x 576
bookshelf: content pos=Offset(192.0, 264.0) size=2208 x 576   ← 全程恒定，无跳动
```

（`innerPadding` 的 left/right 为 0 是正确的：start 侧 inset 由 rail 自己消费，
`NavigationSuiteScaffold` 的 content 槽 `consumeWindowInsets(rail.windowInsets.only(Start))`。）

## 4. 修复要点（已在代码中）

| 位置 | 约束 |
|---|---|
| `ReaderScreen.leaveReader` | 退出阅读页（返回键 / 顶栏返回 / 菜单返回 / 错误页返回）统一走此路径：先 `BEHAVIOR_DEFAULT + show(systemBars)`，再由 `BarsRestoreGate` 等**四边**快照等于 `getInsetsIgnoringVisibility(systemBars)` 且连续两帧稳定才导航；800ms 超时兜底（日志 `gate TIMEOUT` 即仍有跳动风险） |
| `FoldReaderApp` | 只用 `layoutType`（`NavigationSuiteType.None` 零尺寸立即生效）切换导航组件，禁止用 `NavigationSuiteScaffoldState.snapTo` —— 其内部 `animateFloatAsState` 会逐帧挤压内容 |
| `BarsRestoreGate` | 不可退化为"某一边非零即放行"或"快照稳定即放行"：`show()` 刚发出时全零也可能稳定数帧 |

## 5. 历史版本为何仍跳

- `037dc7b` 的旧门控只等**状态栏 top** 非零（`snapshotFlow { statusBars.getTop() > 0 }`，200ms 超时）：
  横屏下 top（状态栏）先到就放行，侧边导航栏的 left/right inset 随后才到 → 仍右跳。
- 修复随 `BarsRestoreGate` 引入，但该提交/改动若未重新编译安装（例如安装的是 `app/build/outputs/apk/release`
  里的旧包），设备上仍是旧行为。

## 6. 回归判定（真机快速自查）

```bash
adb logcat -c && adb logcat -s ReturnTrace
# 进书 → 返回书架，观察：
#  1) leaveReader: gate passed after N frames        ← 不能是 TIMEOUT
#  2) bookshelf: content pos=Offset(x, y) size=...   ← 从首帧起 x/y/size 恒定不变
```

## 7. 残留问题（进书方向）已随本次重构消除

打开书籍时方向相反：此前 `layoutType` 切到 `None` 会让尚在退场的书架内容整体左移
（实测 192→72px）。现阅读页已移出外壳 content 槽（见第 8 节），外壳形态与路由无关，
进书时书架只做 `scaleOut(0.94)+fadeOut`，**不再重排**（`content size` 全程 2208×576 恒定）。

## 8. 结构性修复：阅读页全窗口覆盖层（ReaderOverlay）

```
SharedTransitionLayout
└─ Box(fillMaxSize)
   ├─ NavigationSuiteScaffold(layoutType = 顶层形态，恒定)   ← rail 宽度只随系统栏 inset 变
   │  └─ FoldReaderNavHost: bookshelf / settings（+ reader 空占位）
   └─ ReaderOverlay(AnimatedContent, 整窗)                  ← 阅读页实际画面
```

- 阅读页不再受 content 槽宽度约束：进书时它从第一帧就是整窗宽高，**不会先窄后宽重排**；
- 书架退场期间外壳槽位不变 → 不再左移；
- 退书时 rail 早已在覆盖层背后恢复到位，书架首帧即终局（配合 `BarsRestoreGate`）；
- 封面共享元素仍成立：书架与覆盖层同属一个 `SharedTransitionLayout`，
  覆盖层把 `AnimatedContentScope` 作为 `animatedVisibilityScope` 传给 `ReaderScreen`；
- 阅读页的 `ViewModel`/`rememberSaveable`/`Lifecycle` 由覆盖层按 NavHost 的方式提供
  `LocalViewModelStoreOwner/LocalLifecycleOwner/LocalSavedStateRegistryOwner = readerEntry`
  ——阅读时长仍在该 entry 弹栈（`onCleared`）时落库（实测 bookId=19：48166ms → 70351ms → 79526ms）。

### 8.1 必须遵守的 entry 生命周期约束（踩过的坑）

NavHost 只在**转场跑完**时调用 `onTransitionComplete(entry)` 才把 entry 置为 `DESTROYED`。
阅读页目的地在 NavHost 里只剩空占位，若其转场时长为 0（`EnterTransition.None`），entry 会在
pop 的下一帧就被销毁，而覆盖层还在组合退场内容 → 阅读页每帧都在读 `animatedVisibilityScope.transition`
而重组 → `viewModel()` 访问已销毁 entry 的 ViewModelStore：

```
java.lang.IllegalStateException: You cannot access the NavBackStackEntry's ViewModels
after the NavBackStackEntry is destroyed.
    at ...NavBackStackEntry.getViewModelStore
    at androidx.lifecycle.viewmodel.compose.viewModel
    at ...ReaderScreen(ReaderScreen.kt:2275)
```

因此：占位转场用 `fadeIn/fadeOut(tween(700))`（`readerPlaceholderEnter/Exit`），
时长 ≥ 覆盖层退场弹簧的收敛时间（~420ms）；覆盖层再按 `entry.lifecycle` 是否
`≥ CREATED` 兜底停止组合，双保险。

## 9. 回归判定（真机 / 模拟器快速自查）

```bash
adb logcat -c && adb logcat -s ReturnTrace
# 进书 → 返回书架：
#  1) shell: slot pos=Offset(x, 0) size=...    ← 进书瞬间不应变化（书架退场不重排）
#  2) leaveReader: gate passed after N frames  ← 不能是 TIMEOUT
#  3) bookshelf: content pos=Offset(x, y) ...  ← 返回后从首帧起恒定
#  4) 无 FATAL EXCEPTION（重点看 NavBackStackEntry 的 IllegalStateException）
```
