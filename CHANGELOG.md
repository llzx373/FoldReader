# Changelog

本文件记录 FoldReader 的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [1.0.0] - 2026-09-18

首个公开版本。折叠屏上的电子书 / 漫画 / PDF 本地阅读器，完全离线、无任何权限声明。

### Added

- **折叠屏形态适配**：折起单页、展开进入双页书模式（铰链安全区避让、一次翻两页、书脊阴影、分侧页眉页脚）、半开进入桌面模式（横铰上下分区，下半屏为控制面板）。折叠/展开/旋转全程保持阅读位置连续。
- **排版与分页引擎**：基于 `StaticLayout` 的逐行排布分页，支持中文避头尾、两端对齐、首行缩进、字距/行距/段间距、每行字数与页边距档位；分页边界走内存 LRU + 磁盘双层缓存。
- **TXT 格式引擎**：编码自动识别（UTF-8 BOM/无 BOM、UTF-16 LE/BE、GBK、GB18030、Big5）、大文件窗口化字节偏移读取、偏移索引与章节规则库。
- **EPUB 支持**：EPUB3 NAV 与 EPUB2 NCX 目录、元数据、封面提取、富文本渲染、目录层级保留与回填自愈。
- **FB2 支持**：裸 XML 与 `.fb2.zip` 两种形态。
- **漫画阅读模式**：zip/rar/tar/7z 容器（`.cbz`/`.cbr`/`.cbt`/`.cb7` 及通用扩展名）、图片文件夹；按真实魔数判容器；从右往左的日漫方向；适应整页/宽度/高度/原始尺寸四种缩放；缩略图快速跳页。
- **PDF 阅读**：文本型 PDF 可按普通电子书阅读（含排版与全文搜索），扫描件走页式渲染；渲染由 androidx.pdf 的沙箱文档服务完成。
- **同系列上下卷切换**：自动匹配当前目录/分组中的同系列书籍，一键切换上一卷/下一卷。
- **页内锚点**：书签与批注可精确到页内位置（点或矩形），文本、漫画与 PDF 共用同一模型，支持同一页多个书签。
- **可配置中间点击区**：单击与双击各自绑定动作（切换菜单 / 上一页 / 下一页 / 书签 / 缩放 / 无）。
- **阅读辅助**：多级目录、书签与全书签总览、多色高亮与笔记、按章节分组的全文搜索、自动翻页（定时/滚动双模式）、阅读计时与按天分桶的阅读统计、5 套内置主题加自定义配色、应用内亮度与屏幕常亮、页眉页脚。
- **书架**：网格/列表视图、自动生成的书籍封面与漫画封面、分组、稳定排序、批量导入、封面就绪角标、导入后后台预热。
- **导入流水线**：SAF 与「打开方式」（`VIEW` intent）两条入口、内容哈希去重、目录成组导入、内置文件浏览器、导入时可选清理（空行、自定义正则广告行、繁简转换）。
- **数据与诊断**：本地 JSON 备份恢复、应用内诊断日志（落盘 + 崩溃转储 + 设置页分享）、「关于」区与开源许可清单。

### Changed

- 阅读链路两轮性能优化：渲染、开书、搜索、索引与存储；滚动模式拆分状态并批量预取。
- 持久化偏移索引改为双列模型，根治代理对（surrogate pair）跨块导致的索引错位。
- 翻页模式切换改为立即生效；系统栏可见性统一策略，消除进出阅读页时的 inset 跳变。
- 摄像头规避泛化为按 `displayCutout` 自动检测（右上/左下）。
- 数据库基线重置为 `version = 1`，**不提供迁移**；预发布阶段只保证全新安装。升级版本请先使用应用内备份。

### Removed

- **仿真翻页（卷曲几何）相关代码与数据被完整删除**。保留 COVER（覆盖）、NONE（无动画）、SCROLL（滚动）三种翻页方式。

### Fixed

- 从阅读页返回书架时封面与右上/右下按钮整体右跳（阔折叠内屏横屏 grid 必现）：门控改为比对外壳 inset 全集并抑制平台补报的瞬时挖孔 inset。
- 阔折叠的双页判定与进书时的单双页跳变。
- EPUB 目录回填失败可自愈，并保留目录层级。
- 漫画 RTL 方向翻译统一、中间区几何按是否有底边条区分。
- 每书/全局阅读偏好的边界处理、并发写盘与 PDF 渲染尺寸。
- 系统栏恢复时机与版式切换时的几何混绘、字距爆炸。
- 日期/时间文案在 Composable 内直接读 `Locale.getDefault()`，不可观察：应用存活期间切换系统语言后，
  书籍详情、书签列表、全书签总览、书架副标题与阅读器时钟的文案不会重排。改为经由配置对象读取语言。

[Unreleased]: https://github.com/llzx373/FoldReader/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/llzx373/FoldReader/releases/tag/v1.0.0
