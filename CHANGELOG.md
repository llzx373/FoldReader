# Changelog

本文件记录 FoldReader 的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- **AI 章节规则生成全流程**（M15 收官）：目录对话框「未识别到章节」分支与书籍详情页新增「AI 识别章节」入口（仅 TXT 书籍且 AI 服务已配置时显示，未配置时界面零变化）。流程：读取全书正文 → 粗筛采样疑似标题行（≤300 行）→ 首次外发前一次性确认（明示服务商地址与数据范围，确认后不再弹）→ AI 归纳 1~3 条候选正则 → 本地逐条真实试切预览（章节数、前 20 标题、超长章/空章/孤儿文本异常标记）→ 选定后存为本书自定义章节规则并自动重建目录。每次外发记入「外发历史」台账（feature=章节规则、scope=书名、按采样字符数/2 估算 token）；重建目录链路上移至容器，书架「重建目录」与 AI 选定后重扫共用。
- **内置提示词底座与「章节规则生成」提示词**（M15 起步）：新增 `core/ai/prompt/` 包——模型按采样出的疑似章节标题行归纳 1~3 条 Kotlin Regex 候选，只输出固定 JSON；解析器容忍散文包裹与代码围栏，JSON 畸形、regex 空白或编译失败的候选一律丢弃（宁缺毋滥，绝不抛异常），候选数截断到 3 条；`BuiltinPrompts` 集中登记内置提示词全文，为设置页「内置提示词」只读查看（14.2 遗留项）备下枚举入口。
- **AI 服务设置与三协议底座**：设置页新增「AI 服务」分区（设置 → AI 服务），支持 OpenAI Chat Completions / OpenAI Responses / Anthropic Messages 三种协议的服务商接入，含通用/翻译/视觉模型分别配置、连接测试与外发历史；API key 经 Android Keystore 加密存储，不进备份与诊断日志；未配置时应用不产生任何网络请求。
- **2.5D 仿真翻页**（可选，默认仍是覆盖）：微信读书同源的折痕反射（垂直平分线 + 二次贝塞尔 + Householder 等距镜像），翻起的是一张不变形的平纸，不恢复已删除的圆柱/铰链 3D 模型。电子书与漫画 / 页式 PDF 共用同一套几何；展开双页只卷被抓住的那一叶，后半段绕书脊盖住对页。日漫下逻辑前进掀左叶，与覆盖动画同一口径。阅读菜单与设置里增加「仿真」。
- **竖向滑动 = 在起手那一列「点一下」**：分页模式下竖向滑动此前是无反馈的死区——竖向一旦赢下触摸 slop，横向检测器就放弃了，而竖向只绑了「亮度手势开启且起手在左 1/3」。现在不在亮度热区内的竖滑等价于在起手处点击：左区回上一页、右区到下一页、中间区走该区配置的动作，与点击共用同一个入口（`handleTap`），日漫方向翻译一并沿用。亮度热区同时收窄到**最左侧 1/6**（`BRIGHTNESS_EDGE_FRACTION`），否则左区竖滑永远只能调亮度。滚动模式下竖向仍是滚动。
- **文本阅读器的顶栏与底部菜单补上进出动画**：原先是裸 `if` 直接挂载/卸载、瞬现瞬隐，而漫画阅读器对同一交互早有动画。按漫画的写法统一（220ms，淡入 + 从各自边缘滑入）。

### Fixed

- **电子书双页仿真时中缝阴影只剩一边**：书脊改画在掀纸上面，只扣掉翻起的那片；被掀那一叶靠书脊的半边还在，翻过去的部分由纸背盖住。
- **漫画打开双页会来回闪**：解码目标在尺寸未稳定（<64px）或只有几个像素抖动时不再整表清掉重解；版式与 ViewModel 的双页状态对齐后再分栏。
- **漫画双叶仿真纸背/下一页是空白**：预取窗口加大到下下一页，等目标页解码完再烘位图，缺页就退回瞬时翻页而不是掀一张白纸。
- **横屏仿真要拖很远才翻页**：起手不再额外加 8px slop；完成阈值改按叶短边 15%，并用横滑判定距离（默认 40dp）封顶。
- **双页从右边翻到尽在中缝就切页**：终帧触点落到对页远角，纸背先盖住另一叶再换页。
- **单页仿真掀起的纸背上印着反字**：单页按单面印刷处理，纸背只留底色和阴影；双页仍用下一开对侧叶当纸背。
- **漫画双页仿真卡住、偶发变成单页掀**：双页配对不再等解码尺寸；布局抖动几个像素不再作废进行中的掀页；相邻页并行等待解码；预取改为当前跨页再前后各两页，淘汰下限提到 8 张。掀页开始时锁死左右叶与位图，覆盖层不再每帧重算；下一开落单仍掀一叶，不退回整幅单页。
- **双页终帧走对角线**：右下角落到对页左下、右上角落到对页左上，不再斜着去对页另一角。
- **漫画仿真翻页起手要拷三张叶尺寸位图**：静图直接引用解码缓存，绘制时按适应模式 letterbox；覆盖层结束时也不 recycle 这些引用。GIF / 动态 WebP，以及必须把左右两页合成一张叶的情况，仍烘离屏拷贝。电子书正文没有现成页图，继续烘离屏位图。
- **仿真掀页偶尔「滑了没翻页」，并卡在半掀**：掀页起步要先把当前页与目标页烘成位图（漫画侧 `awaitPage` 最长 800ms），是异步的；而抬手是独立回调、只看 `phase`。抬手早于起步落地时它判定"没在拖"直接放弃——这一次翻页被丢掉，随后起步才把 `phase` 置成 `Drag`，此时再没人调 `endDrag`，掀页就永久停在半掀。现在起步函数返回 `Job`，抬手与取消都先 `join()` 再判定。这也解释了为什么以前"卡住之后连点也没用"：`turn()` 里的 `peel.busy` 守卫会把点击一并挡掉，只能靠折叠/旋转触发几何变化脱困。
- **翻页动画进行中连点会丢掉翻页**：动画在跑时下一次点击被直接丢弃，而阅读本身就是连点（覆盖 220ms / 仿真 400ms 的窗口期）。改为互斥量串行排队，点击次数与翻页次数一一对应；`target` 在锁内计算，否则排队中的点击会各自基于同一份旧页号、连点 N 次只前进 1 页。
- **仿真掀页抬手后轻甩和慢拖手感一样**：抬手后原本是固定 400ms 的 `tween`，手指离手那一刻的速度被丢掉。Compose 的 `tween` 会忽略 `animateTo` 的 `initialVelocity`（只有弹簧消费它），因此完成路径换成临界阻尼弹簧并直接传入抬手速度；取消路径保持固定时长——系统纠正用户的动作应当干脆。
- **顶层导航有四个转场槽位回落到默认 700ms**：书架只定义了退场与返回进入，浏览与设置只定义了进入与返回退出，其余方向走 NavHost 默认的 `fadeIn / fadeOut(tween(700))`，比全 App 节奏（220–420ms）慢一倍多、且与相邻方向不是一套语言。四个槽位补齐，复用既有的 spring token。
- **返回书架不镜像出程**：进书是 `scaleOut(0.94) + fadeOut`，返回却只有 `fadeIn`。补 `scaleIn(initialScale = 0.94f)`（`initialScale` 不能省——`scaleIn()` 默认从 0 缩放）。
- **进书时整屏自右滑入与封面共享元素互相打架**：加载态的封面是共享元素、会自己从书架格飞到屏幕中央，而外层又整体平移一整屏，两股位移在推同一个封面。去掉整屏滑动改为纯淡入，退场同理改纯淡出，保持去程回程对称。
- **关掉亮度手势后竖向滑动完全没有响应**：两个阅读器里那段竖向 `pointerInput` 会因 `brightnessGestureEnabled = false` 整段提前 `return`，连左 1/3 也不挂。现在只挡滚动模式与选区，开关下沉到"这一次算不算亮度"的判定里。

## [1.1.0] - 2026-09-19

### Added

- **横滑翻页阈值可调**（设置 → 翻页与交互）：「横滑判定距离」（20–80dp，默认 40dp）与「横滑判定速度」（200–1200dp/s，默认 500dp/s）。两者取或——滑够远、或甩得够快，都算翻页；两个阅读器共用同一套判据，改完立即对已打开的书生效。
- **网络小说智能清理**（离线规则引擎 + 测试集）：针对从网上下载的 TXT 做确定性清洗，解决空白混掺、行尾空格、段中空行、硬换行把一段拆成多行、章节名出现在行中间、章节命名混乱、引号内被空格/换行拆开、文字被星号遮蔽、网站宣传语、论坛复制残留，以及 HTML 实体与标签、标点不规范、正文开头混入目录块、行内水印、装饰分隔线等共四层问题。
  - **三档预设 + 14 项细粒度开关**：保守（只做无争议的字符/空白归一与广告行过滤）、标准（默认，再加段落重组、空行规整、章节修复、标点规整）、激进（再用重复标点折叠）；也可以在设置页逐项调整（档位随之落到「自定义」）。
  - **导入时可选 + 清洗预览**：导入对话框里选档位、单独开关繁简转换，并可先看一份采样 256KB 的改动报告（计数 + before/after 样例）再决定。
  - **已导入的书可「智能整理」**：读原始源文件重新清洗、重建目录，并会明说进度与书签的字符位置可能变化。档位在对话框里**当场可选**，改一次就重新预览一次；因此可以对同一本书**反复换规则重洗**——每次都从原文跑，结果只取决于「原文 + 本次规则」，不会在上一版副本上叠加。选「不清理」即**撤销清理**（删副本、恢复原始编码，回到直接读原文件）。
  - **设计上保证幂等**：所有判据只看相邻两行（不按全文比例判定），清洗两遍结果不变。
  - **导入清理时原版与清洗版各占一个书架条目**：同一个源文件在书架上是两条（两行共用同一份源副本，不额外占空间），详情页写明「正文：原文件 / 清洗副本」，书架上有「已清洗」角标——清洗是可逆的用户选择，「洗过之后还想看原文」不必重新导入一次。取文按 bookId 解析，点哪一本就读哪一份。重复导入同一档位时直接打开已有的清洗版。
  - **测试集** `app/src/test/resources/novel-corpus/`：23 条正向用例（`input.txt`/`expected.txt` + 人读说明）与 6 条反向用例（已排好的文本必须逐字节不变），外加全语料幂等断言。
- 全局偏好「清理档位 / 清理规则明细」（设置 → 智能清理），并纳入备份导出/导入。
- **「分享」入口**：注册 `SEND` / `SEND_MULTIPLE`，可以在系统分享面板里把电子书或漫画直接送给 FoldReader（与「打开方式」走同一条导入流程）。
- **支持的格式清单成文**：TXT / EPUB / FB2 / PDF 与漫画容器（CBZ / CBR / CBT / CB7 及直接当容器的 zip / rar / tar / 7z、图片目录）的扩展名与 MIME 对应关系写进 `docs/需求与设计说明书.md` 6.2，同时补齐清单与文件选择器里漏掉的条目。
- **导出正文 / 导出清洗后文本**（书籍详情，TXT）：把库里那一份另存到用户选的位置（系统「另存为」），用来直接对照清洗前后——清洗副本在应用私有目录里，看不见摸不着，只看阅读页很容易看走眼。有清洗副本时两个入口都给，没有时只有「导出正文」。
- **正文来源写进诊断日志**：每次打开 TXT 记一行 `reader: bookId=… 正文=私有文件:<hash>.txt / 外部源:<scheme>`，把"读到的还是原文"这类问题从靠猜变成可查（文件名是内容哈希，不带用户的文件名）。

### Fixed

- **横滑翻页要「滑很长」才生效**：判定原先写死为屏宽的 15%（展开态约 110–130dp，远超拇指一次自然滑动），于是「正常滑一下」正好落在触摸 slop 与阈值之间的死区——手势在、却毫无反应。现在改**固定位移阈值（40dp）**并补一条**甩速判据（500dp/s）**：位移过线或短促轻甩都翻页，方向按位移/脱手速度定；文本与漫画两个阅读器共用同一套判据（`horizontalSwipeDirection`），日漫的左右镜像不变。
- **清洗结果读不到：「智能整理」重洗完，阅读器里还是原文**。阅读取文是按**源文件 URI** 反查这本书的（`contentUriResolver` 与 `bookIdResolver` 各自独立查一次 `findByFileUri`），而同一个 `fileUri` 在库里可以有多行（原版 + 清洗版），`getByFileUri` 又是 `LIMIT 1` 且无排序——于是打开清洗版那一本，正文、偏移索引与章节都可能落到原版那行上。现在取文一律按调用方给出的 bookId 解析（`BookParser.openContent` / `parseChapters` / `prewarm` 增加 bookId 入参，EPUB/FB2/PDF 的章节回填同样透传，预热的 bookId 来自队列里的任务本身），`getByFileUri` 补 `ORDER BY id` 作为「不知道 bookId」时的确定性兜底。
- **「智能整理」重洗后，已经打开着的阅读器不会换正文**。阅读器原先只订阅 `book.encoding` 的变化，而重洗写入的编码恒为 UTF-8——对本来就是清洗副本的书是同值，`distinctUntilChanged` 直接吞掉。现在同时订阅 `cleanedFilePath`，副本一换就重开正文（撤销清理同样触发）；`ReaderHost` 也改为订阅书行，不再只在 bookId 变化时取一次。
- **同一个源文件的「原版」与「清洗版」在书架上长得一模一样**，分不清点开的是哪一本。TXT 的清洗副本加「已清洗」角标（网格与列表都有），详情页再加一行「正文：原文件 / 清洗副本」。
- **「浏览」打开与「导入目录为分组」永远不清洗**，同一个文件换个入口进来结果就不一样。清洗配方的组装收到一处（`feature/importer/CleanProfileFactory`），这两个入口跟随设置页的档位与繁简偏好；书架导入对话框仍按当次选择。浏览打开大文件时会显示清洗进度，不再是干等的「正在打开…」。
- **外部「打开方式」进来后直接落到书架，什么都不发生**。入口只认 `ACTION_VIEW` + `intent.data`，别的 action 一律忽略，清单也漏了 `application/pdf`、且没有注册分享过滤器。现在 `VIEW` / `SEND` / `SEND_MULTIPLE` 都收，URI 从 `data` / `clipData` / `EXTRA_STREAM` 三处取；一次送来多个文件会排队逐个确认；App 当时停在浏览或设置页会先切回书架再弹导入对话框（此前导入对话框长在书架上，不切回去就只是"没反应"）。同时按 scheme/authority 记一行诊断日志——这类"选了 FoldReader 却没动静"的问题从此有现场可查。
- **外部「打开方式」导入的书，任务一结束就打不开**。外部给的 `content://` 是**临时**授权，任务结束（在最近任务里划掉、或重启手机）即失效，而「不清理」导入的正文正是每次打开现读它——失效后再点开只有一屏「无法打开书籍 / Permission Denial」。现在导入时把**原文原样复制一份**进 `filesDir/source/`（按原文内容哈希命名，重复导入直接复用），`fileUri` 指向这份副本；TXT 与 EPUB/FB2 都覆盖，于是阅读、重洗、撤销清理、切编码只碰私有目录里的文件。同一个文件的原版与清洗版**共用同一份**源副本，删书因此改成「行删完再判还有没有别的书引用它」。漫画与 PDF 仍只登记外部源（体积大，漫画另有显式的「复制到本地」）。
- **删掉一本「清洗版」会把另一本的正文一起删掉**。清洗产物是按**内容**命名并复用的：同一个源文件的「原版」与「清洗版」用同一套规则重洗时会落到同一个 `cleaned/<哈希>.txt` 上，而删书原先是无条件删 `cleanedFilePath`——被牵连的那一本此后打开只有一屏「无法打开书籍」。现在与源副本同样处理：先把文件路径收集成候选，等行删完再反查 `BookDao.getByCleanedFilePath`，确认没有别的行还在引用才真删。
- **「导入本地书籍」的文件选择器少列了格式**：PDF 与全部漫画 MIME（cbz/cbr/cbt/cb7 及各压缩包写法）没进去，这些文件在选择器里根本选不到。
- **FB2 只有扩展名判定、没有 MIME 判定**：来源不给 `.fb2` 后缀而报 `application/x-fictionbook+xml` 时，文件浏览器与批量导入都看不到它。判定与「可导入」白名单都补上这两种写法。
- **智能清理：顶格的分篇标题被并进正文第一行**。合集文件的分篇标题（`【外传·某某篇】（某某篇）作者：某某`）**顶格**写，而这类文件的正文也顶格写：缩进判据没有缩进可看，标点判据又因为标题行不以句末标点收尾、且超过硬换行的 16 字线，把它判成「长行被硬换行切断」，标题连同中间的空行一起并进了正文首行（实测一本 296KB 的合集里 2 处，输出成 106 字与 80 字的顶格长行）。新增判据：整行以 `【…】` 起头、不含句末标点、不超标题长度即自成一体，两侧都不参与段落重组。它与章节标题判据**刻意分开**——那个判据空行规整也在用，并进去会把标题前的空行一并删掉。
- **智能清理：缩进分段的书里，句末标点之后的续行没被并回**。像 `『原来你早就知道了。』` 后面紧跟 `他沉默着没有回答`——引号闭合了，但这段话还没完。旧判据「上一行以句末标点结尾就不合并」在这种书上把段落切成了一段段短线（实测一本 296KB 的合集里有一百多处）。现在按缩进与标点分路判断：这一段用缩进起头时，「下一行没有缩进」是权威续行信号，标点不作数；没有缩进可依赖时才用标点与行长判断。
- **智能清理：「每一行都带缩进」的排版版一行都合不上**。下载站精校版常见这种写法——全篇**每一行**都缩进（续行也缩进）、行与行之间还留一个空行（双倍行距），段落被硬换行切成一段多行。旧判据里「有缩进 → 新段落」先把所有续行否决掉，就算绕过它，「缩进分段」那条路又要求两行之间没有空行，于是整本两千多个非空行**合并 0 次**。现在下一行带缩进时不再一票否决，改为看**上一行有没有说完话**：行尾是句末标点才是新段落；没说完、且达到 16 字下限（或引号未闭合）的就是续行，空行也不再当边界。安全边界正是「收尾」这一条——正常书的段落必以标点结尾，所以行为一个字节都没变，新增反向用例钉住这一点。
- **智能清理：删空行把「顶格分篇标题」并进了上一段**。「缩进段 → 空行 → 顶格标题」这种形态里，空行是「顶格行不是续行」的**唯一**依据；空行规整原先只看两侧有没有缩进，把它当冗余删了，重组随即把标题并掉。而且**原文的空行个数会影响结论**（3 个空行时「不并」、折成 1 个后「并」），因此不幂等。现在空行规整与段落重组共用同一套边界判据：空行只有在「下一行能自己站稳」（有缩进 / 是标题）时才冗余。
- **智能清理：行中标题提行时丢了正文侧的段首缩进**，段落起点信号随之丢失（再次清洗结果会变）。提行时把原文的行首空白一并带上。
- **智能清理：`第 2 章` 这种带空格的标题会被并进上一段**，标题再也提不出来。段落重组的标题守卫改为认「规范化后也是标题」的写法。
- **智能清理：被换行切断的省略号会多出一倍**。`…` 与 `…` 分处两行时各自被补成 `……`，拼起来变成 `…………`。标点规整挪到段落重组之后，先拼回再规整。
- **智能清理：断成两截的分隔线漏网**（`----- -----` 中间夹着空格），随后被标点规则变成 `—— ——`，像正文里多了一句破折号。装饰线规则改为允许行内空白。
- **智能清理：文件头的 `作者：xxx` 被当广告行删掉**，丢失作者信息。作者行规则不再匹配裸的「作者：」。
- **智能清理：全角缩进在关闭「字符归一」时不被认作缩进**，段落起点被当成续行、把整段粘成一坨。缩进判定改为与空白规则共用同一套（不再只认半角空格）。
- **空白归一化**（阅读器菜单开关，每书独立）：折叠段首/行尾/行内多余空白与 BOM、零宽空格/连接符、软连字符等不可见字符，段首统一改用标准首行缩进——网页抓取来的 TXT 不再顶着几个空格、段与段对不齐。实现只折叠**绘制宽度**、不改动任何字符，选区、划线、书签与搜索命中的偏移零影响。
  > 与上面的智能清理互补：它只改**绘制**（阅读期、可随时关、不动字符），智能清理改的是**内容**（导入/整理期、删广告与重组段落）。

### Changed

- **数据库恢复向前兼容**：`version` 升到 2 并登记 `MIGRATION_1_2`（`book_prefs` 新增「空白归一化」列），`v1.0.0` 的老库原地升级、不丢数据；废止"只保证新安装、不写迁移、重置 schema 基线"的预发布策略，已发布版本的 schema 快照一律保留。
- 备份导入不再因为版本号比本机新就整份拒绝：只拒绝认不出的版本，更新版本导出的备份按已知字段尽力导入、未知字段回落默认值。
- 阅读器菜单底部的开关区改为自动换行布局，中文标签不再被挤成逐字竖排；编码与开关按钮的内边距同步收紧。
- 导入清理由「去空行 / 去广告行 / 繁简转换」三个复选框改为**档位选择**；旧的 `TextCleaner` 被新的 `core/format/clean/` 引擎取代。批量导入与文件浏览器仍按原样导入（不清洗）。

### Removed

- `core/format/TextCleaner.kt`（能力并入 `core/format/clean/`，`TsCharMap` 随迁）。

## [1.0.0] - 2026-09-18

首个公开版本。折叠屏上的电子书 / 漫画 / PDF 本地阅读器，完全离线、无任何权限声明。

### Added

- **折叠屏形态适配**：折起单页、展开进入双页书模式（铰链安全区避让、一次翻两页、书脊阴影、分侧页眉页脚）、半开进入桌面模式（横铰上下分区，下半屏为控制面板）。折叠/展开/旋转全程保持阅读位置连续。
- **排版与分页引擎**：基于 `StaticLayout` 的逐行排布分页，支持中文避头尾、两端对齐、首行缩进、字距/行距/段间距、每行字数与页边距档位；分页边界走内存 LRU + 磁盘双层缓存。
- **TXT 格式引擎**：编码自动识别（UTF-8 BOM/无 BOM、UTF-16 LE/BE、GBK、GB18030、Big5）、大文件窗口化字节偏移读取、偏移索引与章节规则库。
- **EPUB 支持**：EPUB3 NAV 与 EPUB2 NCX 目录、元数据、封面提取、富文本渲染、目录层级保留与回填自愈。
- **FB2 支持**：裸 XML 与 `.fb2.zip` 两种形态。
- **漫画阅读模式**：zip/rar/tar/7z 容器（`.cbz`/`.cbr`/`.cbt`/`.cb7` 及通用扩展名）、图片文件夹；按真实魔数判容器；从右往左的日漫方向；适应整页/宽度/高度/原始尺寸四种缩放；缩略图快速跳页。
- **PDF 阅读**：文本型 PDF 可按普通电子书阅读（含排版与全文搜索），扫描件走页式渲染；渲染由 androidx.pdf 的沙箱文档服务完成。
- **同系列上下卷切换**：自动匹配当前目录/分组中的同系列书籍，一键切换上一卷/下一卷。
- **页内锚点**：书签与批注可精确到页内位置（点或矩形），文本、漫画与 PDF 共用同一模型，支持同一页多个书签。
- **可配置中间点击区**：单击与双击各自绑定动作（切换菜单 / 上一页 / 下一页 / 书签 / 缩放 / 无）。
- **阅读辅助**：多级目录、书签与全书签总览、多色高亮与笔记、按章节分组的全文搜索、自动翻页（定时/滚动双模式）、阅读计时与按天分桶的阅读统计、5 套内置主题加自定义配色、应用内亮度与屏幕常亮、页眉页脚。
- **书架**：网格/列表视图、自动生成的书籍封面与漫画封面、分组、稳定排序、批量导入、封面就绪角标、导入后后台预热。
- **导入流水线**：SAF 与「打开方式」（`VIEW` intent）两条入口、内容哈希去重、目录成组导入、内置文件浏览器、导入时可选清理（空行、自定义正则广告行、繁简转换）。
- **数据与诊断**：本地 JSON 备份恢复、应用内诊断日志（落盘 + 崩溃转储 + 设置页分享）、「关于」区与开源许可清单。

### Changed

- 阅读链路两轮性能优化：渲染、开书、搜索、索引与存储；滚动模式拆分状态并批量预取。
- 持久化偏移索引改为双列模型，根治代理对（surrogate pair）跨块导致的索引错位。
- 翻页模式切换改为立即生效；系统栏可见性统一策略，消除进出阅读页时的 inset 跳变。
- 摄像头规避泛化为按 `displayCutout` 自动检测（右上/左下）。
- 数据库基线重置为 `version = 1`，**不提供迁移**；预发布阶段只保证全新安装。升级版本请先使用应用内备份。

### Removed

- **仿真翻页（卷曲几何）相关代码与数据被完整删除**。保留 COVER（覆盖）、NONE（无动画）、SCROLL（滚动）三种翻页方式。

### Fixed

- 从阅读页返回书架时封面与右上/右下按钮整体右跳（阔折叠内屏横屏 grid 必现）：门控改为比对外壳 inset 全集并抑制平台补报的瞬时挖孔 inset。
- 阔折叠的双页判定与进书时的单双页跳变。
- EPUB 目录回填失败可自愈，并保留目录层级。
- 漫画 RTL 方向翻译统一、中间区几何按是否有底边条区分。
- 每书/全局阅读偏好的边界处理、并发写盘与 PDF 渲染尺寸。
- 系统栏恢复时机与版式切换时的几何混绘、字距爆炸。
- 日期/时间文案在 Composable 内直接读 `Locale.getDefault()`，不可观察：应用存活期间切换系统语言后，
  书籍详情、书签列表、全书签总览、书架副标题与阅读器时钟的文案不会重排。改为经由配置对象读取语言。

[Unreleased]: https://github.com/llzx373/FoldReader/compare/v1.1.0...HEAD
[1.1.0]: https://github.com/llzx373/FoldReader/releases/tag/v1.1.0
[1.0.0]: https://github.com/llzx373/FoldReader/releases/tag/v1.0.0
