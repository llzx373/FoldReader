# 03 段落中间有空行（问题 3）

- **现象**：同一个段落被硬换行切成两半，中间还被插了一个空行。
- **期望**：空行被吞掉，两个半句拼成一段；段落之间原本的（单个）空行保留。
- **档位**：STANDARD（`reflowParagraphs` + `collapseBlankLines`）。
- **注意**：空行的去留取决于这本书怎么分段——用缩进分段时空行会被全部删掉，
  用空行分段时只留一个。这本是「空行分段」，所以段落之间的空行保留。
