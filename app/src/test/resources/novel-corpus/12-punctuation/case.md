# 12 标点不规范

- **现象**：省略号写成 `......`（六个半角点）与 `。。。`（三个句号）。
- **期望**：都规范成中文省略号 `……`（两个 U+2026）。
- **档位**：STANDARD（`normalizePunctuation`）。
- **不做**：破折号只处理 `—{2,}`、`―{2,}`、`─{2,}` 与**两侧都不是字母数字**的 `-{2,}`；
  数字区间的连字符（`2020-2021`）不动。
