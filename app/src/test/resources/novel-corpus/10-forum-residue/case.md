# 10 论坛复制残留（问题 10）

- **现象**：楼层号（`3楼: ...`）、论坛用户信息（`UID: 123456`）混在正文里。
- **期望**：这两类整行删除；删掉后原本夹在中间的多余空行收敛成一个。
- **档位**：STANDARD（`filterNoise` 的论坛规则族）。
- **相关**：签名档、发帖时间、标签见 `NoiseRules` 的 `forum-*` 规则（本用例未逐一覆盖）。
