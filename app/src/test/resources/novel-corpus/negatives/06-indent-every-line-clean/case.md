# 反例 06：每行都缩进、但每行都是一句完整的话

- 形态与用例 `23` 相同（每行都带缩进），但这里**每一行都以句末标点收尾**——它们真的是各自独立的段落。
- 期望：**逐字节不变**。
- 这是用例 `23` 那条新判据的安全边界：下一行带缩进时，只要上一行已经收尾，就照旧按「新段落」算。
  正常书里的段落一定以句末标点结尾，所以 `23` 的放宽对它们没有影响——这条用例就是钉死这句话的。
