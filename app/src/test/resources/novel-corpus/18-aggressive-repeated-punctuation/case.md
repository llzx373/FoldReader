# 18 激进档：重复标点折叠

- **现象**：`！！！`、`，，` 这类连打标点。
- **期望**：折叠成一个。`……` 与 `——` **不能**被折叠——它们本来就成对出现。
- **档位**：AGGRESSIVE（`normalizeRepeatedPunctuation`）。标准档与保守档不动它。
- **顺序**：省略号归位（`......` → `……`）必须先于折叠发生，否则 `…` 会被折叠规则误伤。
