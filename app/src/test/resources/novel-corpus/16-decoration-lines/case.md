# 16 装饰分隔线

- **现象**：章节之间的 `☆★☆★☆★☆` 与 `-----------------------------` 分隔线。
- **期望**：整行删除，删掉后前后空行收敛成一个。
- **档位**：STANDARD（`filterNoise` 的 `decoration` 规则）。
- **边界**：`...`（省略号）不在装饰字符集里，会走标点规整变成 `……`，不会被当成分隔线删掉。
