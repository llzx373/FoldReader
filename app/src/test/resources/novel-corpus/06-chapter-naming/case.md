# 06 章节命名混乱（问题 6）

- **现象**：`第 1 章`、`第 2 章` 里数字两侧有空格，内置规则认不出来；
  段首缩进用全角空格，段间夹着空行。
- **期望**：标题规范化成 `第1章 初入江湖`（去掉数字两侧空格、全角数字转半角）；
  段首缩进统一为两个全角空格；本段用缩进分段，所以空行被删除。
- **档位**：STANDARD（`repairChapters` + `canonicalIndent` + `collapseBlankLines`）。
- **不做**：不把 `第1章` 改写成 `第一章`，也不补出缺失的章号——只把已有标题摆正。
