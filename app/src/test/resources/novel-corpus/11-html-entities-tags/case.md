# 11 HTML 实体与标签残留

- **现象**：从 HTML 页面转存下来的文本里还留着 `<br>`、`<p>` 标签与 `&nbsp;` / `&ldquo;` 实体。
- **期望**：标签剥掉、实体解码、解码后产生的空格按问题 1 的规则删掉。
- **档位**：STANDARD（`unifyChars` + `collapseSpaces`）。
- **边界**：`a<b` 这类正文不会被当成标签（要求 `<` 后是字母且成对出现 `>`）。
