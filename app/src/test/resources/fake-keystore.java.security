# JVM 单测追加的 JCA Provider（经 -Djava.security.properties=<本文件> 追加到 JDK 主配置）。
# 编号必须紧跟主 java.security 最后一个 provider（Provider 加载遇编号空洞即止）：
# Windows JDK 25 主配置到 security.provider.10=SunMSCAPI，故这里从 11 开始。
# 若升级 JDK 后 Robolectric 用例报 "AndroidKeyStore not found"，先检查此编号是否仍衔接。
security.provider.11=com.llzx373.foldreader.core.ai.android.FakeAndroidKeyStoreProvider
